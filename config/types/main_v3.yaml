max_pages: 1

layout:
  - description
  - education
  - experiences
  - skills
  - projects
  - certificates

description: "*Platform Engineer* specializing in *Cloud Orchestration*, *Kubernetes*, and *infrastructure automation*. Proficient in *Go*, *Rust*, and *TypeScript* building *high-concurrency tools* and contributing to *open-source ecosystems*. Proven track record in enhancing *system observability*, streamlining *deployment workflows*, and *scaling operations*."

experiences:
  - company: XLSmart
    location: Jakarta Selatan
    role: Platform Engineer
    dates: September 2025 - Present
    job_type: Contract
    points:
      - "*Engineered* a *Go* tool to automate weekly capacity reporting across *~200 AWS/GCP environments*, *slashing manual generation time from 45 to under 3 minutes*."
      - "*Built* a *Go* utility to *audit Kubernetes clusters* at scale, querying *EKS* to gather *PDB* and *HPA* data for cross-team resilience analysis."
      - "*Reduced cloud infrastructure costs by 15%* by engineering automated *Go* scanners to identify and terminate underutilized EC2 instances and detached EBS volumes."
      - "*Developed* 'Kluster-Compare', a *full-stack internal tool* (*Go*, *Next.js*) to visualize and *diff Kubernetes manifests* between live clusters (*Rancher*) and GitOps states (*ArgoCD*)."

  - company: Superbank
    location: Jakarta Selatan
    role: Technical Support
    dates: December 2024 - June 2025
    job_type: Internship
    points:
      - "*Reduced manual deployment effort by 40%* and accelerated release cycles by developing a *PowerShell* automation suite for ZIP extraction and configuration management."
      - "*Resolved critical Tier-1 production incidents* (e.g., CPU spikes, SQL bottlenecks) by analyzing root causes and escalating via *Jira* to cross-functional engineering teams."

projects:
  - name: Terraform Provider TencentCloud (Open Source)
    github: https://github.com/tencentcloudstack/terraform-provider-tencentcloud/pull/4211
    github_handle: tencentcloudstack/terraform-provider-tencentcloud
    description: "Contributed a fix to the official *Terraform Provider for TencentCloud*, addressing an *API pagination bug* in *resource state management*."
    points:
      - "*Identified and resolved* a resource pagination issue by updating the *Go*-based provider logic to *correctly handle and aggregate paginated API responses*."

  - name: Yazi (Open Source)
    github: https://github.com/sxyazi/yazi/pull/3121
    github_handle: sxyazi/yazi
    description: "Contributed to *Yazi*, a lightning-fast terminal file manager written in *Rust*, by implementing real-time progress visualization for I/O operations."
    points:
      - "Developed a *real-time task progression system* for file copying operations, replacing static status messages with visual progress bars and byte transfer metrics."
